#include <stdio.h>
#include "myJump.h"

static struct ctx_s pctx_buf;

int main() {
    int prod;
    printf("Hello");
    prod = mul(0);
    //printf("prod = %d\n", prod);

    //int prod2;
    //prod2 = tryIt(pctx_buf, shintMule, 0);
    //printf("prod = %d\n", prod2);
}
